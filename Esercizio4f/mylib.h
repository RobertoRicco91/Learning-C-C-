#include <stdbool.h>

//chiede un numero all'utente e lo restituisce indipendentemente dal suo valore
int getN();

//calcola la radice quadrata del numero in ingresso
float calcolaRadiceQuadrata(float numero);

//restituisce 1 se il numero � ok altrimenti restituisce 0
bool verificaNumero(float numero);
