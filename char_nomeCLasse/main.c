/*
char_nomeCLasse.c

Data una sequenza di due caratteri (da leggere
uno alla volta con la funzione getchar), stabilire se
la sequenza rappresenta il nome di una classe di
alunni della scuola superiore (esempi: 3C è una
sequenza che indica una classe; C3 oppure 6A non
sono sequenze valide).

Stile di programmazione: Camel Case + Almann
*/

#include <stdio.h>
#include "myLib.h"

void main (void)
{
	char ch, ch2;
	salvaCarattere(ch, ch2);
}
